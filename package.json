{
  "name": "malogist",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:all": "concurrently \"npm run dev:api --prefix backend\" \"npm run dev:worker --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:init": "npm run db:init --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "start:worker": "npm run start:worker --prefix backend"
  },
  "keywords": [],
  "description": "MaLogist fullstack project",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
